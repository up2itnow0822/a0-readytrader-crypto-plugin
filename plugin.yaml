name: readytrader_crypto
title: ReadyTrader Crypto (paper)
description: Paper BTC trading for Agent Zero through the ReadyTrader-Crypto MCP server. Installing the plugin installs the server into the plugin folder, registers it as an MCP server and adds a paper-trading skill. It never enables live trading.
version: 2.0.0
settings_sections:
  - external
per_project_config: false
per_agent_config: false
always_enabled: false
